# Reference clone of the MCP spec repo. Read-only input, never part of the book.
proto/

# The brief and its working drafts. Notes, not book content.
desc
book_outline.md
sample.md

# Reference reading. Third-party, not ours to redistribute.
*.pdf
!docs/*.pdf

# Python
.venv/
__pycache__/
*.pyc

# Node
node_modules/
gallery/**/dist/
npm-debug.log*

# Capture scratch
.capture/

# macOS
.DS_Store

# Editor swap files
*.swp
*.swo
*~

# Local Claude Code settings
.claude/settings.local.json

# Credentials. Never committed, whatever they are called.
# `conf` holds the OpenAI key the eval runner reads; see gallery/evals/openai.mjs.
conf
*.conf
conf.*
.env
.env.*
*.key
*secret*
!gallery/probe/hosts.json
